# Weeb Alexandria 术语表

本术语表用简单、实用的语言说明 Weeb Alexandria 数据库中的信息，以及每个 MCP 工具的用途。

## 数据库中的信息

### Tags（标签）

标签是用来描述图像、角色、风格、动作、物体或概念的词语。例如：

```text
blue_hair
school_uniform
smile
holding_sword
```

标签是搜索信息和构建提示词的基础。

### Categories（类别）

类别说明某个标签属于哪一种信息，例如一般概念、角色、作品、艺术家或其他类型。

### Definitions（定义）

定义解释标签的含义、通常用法，以及与相似标签之间的区别。只要可能，定义都会来自已收录的数据源。由语言模型生成的定义会单独标记为合成内容。

### Aliases（别名）

别名是同一个概念的其他名称，用来连接不同网站或社区使用的名称。

```text
stuck_in_wall → through_wall
```

这表示 `stuck_in_wall` 被视为 `through_wall` 的替代名称。

### Implications（蕴含关系）

蕴含关系表示一个标签通常包含另一个概念。

```text
holding_tripod → holding
holding_tripod → tripod
```

这表示“拿着三脚架”也意味着“正在拿着某物”，并且与“三脚架”有关。

### Popularity and post count（流行度和帖子数量）

帖子数量表示某个标签在来源网站上关联的帖子数量，可作为标签常见程度的近似参考。数量较高不代表标签一定更好，只表示它在该来源中出现得更频繁。

### Sources（来源）

来源是信息所在的网站或数据集，例如 Danbooru、e621、Gelbooru 或 AnimaDex。保留来源可以帮助用户了解定义或关系来自哪里。

### Character tags（角色标签）

角色标签可以代表虚构角色，即使该角色没有结构化的角色档案。通用标签表中的角色标签远多于精选的结构化角色表。

角色表是来自集成 AnimaDex 数据的精选结构化集合，**不代表整个数据库中的角色总数**。许多角色名称可能只作为普通标签存在，并拥有 wiki、别名、流行度或来源信息，但没有结构化角色档案。

结构化角色记录可能包含：

- 角色名称。
- 作品或系列。
- Trigger（识别标签）。
- Core tags（核心视觉标签）。
- Traits（特征）。
- 流行度。
- 相关 LoRA（如果有）。

### Franchises and series（作品和系列）

作品或系列表示角色所属的动画、游戏、漫画或其他虚构世界。在 booru 标签系统中，这类信息也常被称为 `copyright`。

### Triggers

Trigger 是用于识别角色的主要标签或短语，通常是提示词或搜索中引用角色最直接的方式。

### Core tags（核心标签）

核心标签是与角色相关、最有用的视觉标签，例如头发、服装、配饰、颜色或其他明显特征。它们只是起点，不是角色所有外观的完整描述。

### Traits（特征）

特征描述角色的具体属性，例如：

- 头发颜色。
- 头发长度。
- 眼睛颜色。
- 性别。
- 服装或其他视觉细节。

### Artist tags（艺术家标签）

艺术家标签可以代表艺术家，即使该艺术家没有结构化的艺术家档案。通用标签表中的艺术家标签远多于精选的结构化艺术家表。

结构化艺术家记录可能包含艺术家名称、相关标签、触发词、流行度和评分。它们来自集成的 AnimaDex 数据，不能理解为数据库中所有艺术家的总数。

### LoRAs

LoRA 是图像生成模型使用的可选扩展。Weeb Alexandria 为 LoRA 信息预留了位置，但当前数据库快照没有已发布的 LoRA 记录。

### NSFW and SFW

NSFW 表示可能不适合工作场所或普通受众的内容；SFW 表示通常适合普通受众的内容。这些标签帮助理解内容的大致类别，但不取代使用平台自身的规则。

## MCP 工具

### `search_knowledge`

通用搜索工具。它可以搜索标签、角色、艺术家和作品或系列。适合查询范围较大、名称不确定的内容。

### `get_tag_knowledge`

当你知道确切标签时使用。它可以返回定义、来源、类别、流行度、别名、蕴含关系和 NSFW 状态。

### `search_characters`

用于搜索角色，并可以按名称、作品、头发颜色、头发长度、眼睛颜色、性别、流行度或名称排序进行筛选。

### `get_character`

当你知道确切的角色 slug 时使用。它可以返回角色名称、作品、Trigger、核心标签、特征、流行度和相关 LoRA（如果有）。

### `get_sources_status`

用于检查本地数据源和数据库是否可用，适合排查安装或连接问题。

## 如何选择工具

```text
不确定要查什么        → search_knowledge
知道确切标签          → get_tag_knowledge
想搜索角色            → search_characters
知道确切角色 slug     → get_character
检查安装状态          → get_sources_status
```

## 重要限制

- 结果取决于当前安装的本地数据库快照。
- 找不到结果不一定表示该概念不存在。
- 帖子数量来自来源网站，不一定代表所有地方的当前流行度。
- 不同来源的定义和关系可能不同。
- 合成定义应视为建议，而不是来源的官方原文。
